
const Default = "KS.Permissions.Roles";

const rolePermissionDefinitions = {
    getList: Default + '.GetList',
    get: Default + '.Get',
    create: Default + '.Create',
    update: Default + '.Update',
    delete: Default + '.Delete'
}


module.exports = rolePermissionDefinitions;